// State
const state = {
  currentTab: null
}

// Getters
var getters = {
  currentTab: (state, commit, rootState) => {
    let name = rootState.route.name
    
  },
}

// Mutations
var mutations = {
  
}

// Actions
var actions = {

}

export default {
  state,
  getters,
  mutations,
  actions
}
